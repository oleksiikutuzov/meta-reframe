# NetworkManager reads this only for dnsmasq instances belonging to IPv4
# shared connections. Resolve every name to the provisioning web server so
# client connectivity checks discover the captive portal.
address=/#/10.42.0.1
